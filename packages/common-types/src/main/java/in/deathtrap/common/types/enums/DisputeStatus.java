package in.deathtrap.common.types.enums;

/** Lifecycle state of a dispute raised against a trigger or recovery action. */
public enum DisputeStatus { OPEN, UNDER_REVIEW, RESOLVED_PROCEED, RESOLVED_HALT, WITHDRAWN }
